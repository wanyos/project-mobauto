# ─── Base de datos (PostgreSQL) ───
DB_HOST="localhost"
DB_PORT="2345"
DB_USER="mobauto"
DB_PASSWORD="password_segura"
DB_NAME="mobauto"

# URL de conexion (usada por Prisma)
# Se compone de las variables anteriores
DATABASE_URL="postgresql://${DB_USER}:${DB_PASSWORD}@${DB_HOST}:${DB_PORT}/${DB_NAME}"

# ─── Autenticacion (JWT) ───
# Clave secreta para firmar los tokens JWT
# Generar una segura: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET="cambiar_por_una_clave_secreta_segura"
